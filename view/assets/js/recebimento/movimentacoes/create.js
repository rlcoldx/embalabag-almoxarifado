/**
 * Mantido por compatibilidade. O formulário de criação usa form.js.
 */
document.addEventListener('DOMContentLoaded', function () {
    if (!document.getElementById('formMovimentacao')) {
        return;
    }
});
